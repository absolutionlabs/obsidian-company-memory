# Obsidian Setup Skill — Brief

**Status:** Build complete; bundle public on GitHub (`absolutionlabs/obsidian-company-memory`); 11/11 lint PASS; Threat-Map 1-5 OK; skill-prompts ship bundle self-contained. Remaining: Loom recording + operator-side items (DPA / publish privacy + install pages / test matrix / Recovery Drill / private beta).  
**Entity:** Absolution Labs LTD  
**Project Type:** Technical build  
**Created:** 2026-06-02  
**Appetite:** Big — 3-4 sessions (~7-8 hours focused work)  
**Collaboration Mode:** Symbiotic, leaning AI-led on execution. Rob owns judgment calls (UX cuts, public-positioning, what to ship vs cut) and stakeholder questions; Claude owns technical execution (skill code, scripts, templates, tests, docs).

---

## Working Backwards

When this is done, **Absolution Labs LTD** will have a **public, professionally-branded, single-URL-installable skill** that lets **prospective clients install an Obsidian-based company memory themselves in ~25 minutes and experience it as proof that Absolution Labs LTD ships trustworthy, working, secure tools** — without writing a line of code or touching a terminal. The skill is itself the sales pitch; every install is in front of a prospect; per-install quality is absolute.

## Scope

**In:**

- Single source repo, dual install paths: Cowork plugin (URL install) and Claude Code skill (one-line install)
- Single-company vault shape only
- Vault scaffolding: folder structure, `SCHEMA.md`, `index.md`, `log.md`, `CONTEXT.md`, 5-principle starter Operating Principles, `_meta/expectations.yml`, in-vault markdown page templates (`entity.md`, `concept.md`, `query.md`)
- `.obsidian/` config write (app, appearance, core-plugins, community-plugins, hotkeys) with sensible defaults
- 2-question intake (company name + sync location)
- First-screen compliance gate (3 mandatory checkboxes before scaffold)
- Round-trip test (skill creates one entity, updates index, appends log, user verifies in Obsidian)
- Refuse-to-scaffold gate (errors clearly if target directory contains files or existing `.obsidian/`)
- Lint procedure (manual invocation only)
- Phase 2 living guide (`HOW-TO-USE-THIS.md` dropped into vault during scaffold)
- Default-on opt-out telemetry: anonymous UUID, skill version, OS, surface, install success/fail. EU-residency endpoint. No PII.
- Feedback form link in Phase 1 final message
- README with dual install picker, full Absolution Labs LTD branding
- LICENSE (MIT) + comprehensive disclaimer set (all referencing "Absolution Labs LTD" — full legal name)
- Privacy policy at absolutionlabs.com (covering telemetry only)
- `COMPATIBILITY.md` stating tested Obsidian / agent / Dataview versions
- `docs/customisation.md`, `docs/troubleshooting.md`, `docs/upgrading.md` (incl. version-pinned install URLs)
- 3-minute demo video (watch-and-learn the daily flow, anchors trust)
- Test matrix: cold Mac, cold Windows, Dropbox-for-Business account, OneDrive-for-Business account, iCloud account, MDM-managed Windows device, existing-vault user
- Direct-support email + 1-business-day response SLA
- Version-pinned install URLs so users can revert if a future update breaks them
- Cloudflare Worker telemetry endpoint (EU region, rate-limited, payload-validated)

**Out:**

- Installing Obsidian itself (user does this; we link + screenshot it)
- Installing any agent CLI (user installs Cowork / Code / Codex / opencode separately)
- Shipping pinned community plugins inside the bundle (user installs Dataview from Obsidian's browser)
- Templater plugin (confirmed unused by Rob; replaced with in-vault markdown templates)
- Local scheduled lint (manual invocation only)
- First-project folder scaffolding (handed off to `new-project-setup` skill on user's next session)
- Multi-client `clients/<slug>/` vault shape (permanently out per decision #4)
- Git init in vault (cloud sync provides versioning)
- HTML install landing page with surface toggle
- Date format question in intake (auto-detect from system locale)
- Adoption-volume goals (this is a trust tool; volume is not the metric)
- Distribution campaign beyond targeted prospect-list (no LinkedIn launch, no demo blog post for general audience)
- Migration from existing vault (skill refuses if target dir has files; separate migration skill would be future work)
- Custom SCHEMA per user (ship one canonical; users fork if customising)
- Auto-update / version-check warning
- Paid support tier or certified setup partners

## Assumptions to Test

| # | Assumption | What would disprove it | Confidence |
|---|---|---|---|
| 1 | The reframe holds: this skill works as a trust/sales artifact when polished, not as a mass-market product. | Prospects who install it don't form a positive impression of Absolution Labs LTD; sales conversations don't accelerate; word-of-mouth in the prospect's network is neutral or negative. | Medium-high |
| 2 | Cowork's plugin-via-direct-URL install works reliably for non-marketplace plugins on a fresh prospect account. | First test install from a clean Cowork account fails or requires marketplace listing. | Medium-high (likely OK, must verify in Chunk 1) |
| 3 | The "two unavoidable GUI clicks" (Restricted Mode + plugin trust) are acceptable to a non-technical founder in a sales-tool context. | Cold-test prospects bounce at one of these prompts or report being scared. | Medium |
| 4 | A 5-principle starter Operating Principles set lands as approachable, not arbitrary. | Cold-test users either skip the page or report confusion about why those 5 specifically. | Medium-low |
| 5 | Manual lint (no scheduling) gets invoked at meaningful cadence by users who care. | Telemetry / feedback over 6 months shows zero or near-zero lint runs after first session. | Medium |
| 6 | Regulated-sector prospects will install the skill (per decision to test Dropbox-for-Business, OneDrive-for-Business, MDM-managed Windows). | None of the 5 personas in the test matrix succeeds without manual intervention; or regulated-sector prospects all report compliance concerns at the first-screen gate. | Medium |

## Pre-Mortem

The skill shipped and an early prospect — exactly the kind of regulated-sector founder Absolution Labs LTD is positioned to sell to — installed it from the URL we sent them. The Cowork install half-succeeded, leaving a partial `.obsidian/` config and three orphaned markdown files in their Dropbox; or worse, the skill silently overwrote an existing `notes/` folder they'd had for years. Within a week they'd installed our broken artifact, lost data, written client information into a vault syncing to a Dropbox account that didn't carry the DPA their sector requires, and they associated all three failures with "the people who sent me this." The sales conversation that the skill was supposed to anchor was dead before it started, and the prospect (informally or formally) mentioned the experience to one or two peers in the same regulated network.

---

## Threat & Recovery Map

Six surfaces per OP #11. Status at SHAPE close — every `Open` row must transition to `OK` (with evidence) before Gate 1.5 passes.

| # | Surface | Owner | Status | Evidence (target by Gate 1.5) |
|---|---|---|---|---|
| 1 | **Secrets** — skill bundle ships no user-side secrets; our-side: Supabase service-role key + DB password. | Rob (Absolution Labs LTD) | **OK** | Service-role key + DB password in 1P (`op://API Keys/Supabase - Obsidian Telemetry - *`); anon key is public-by-design (Supabase RLS model); pre-publish secret scan returns no JWT or password patterns in committed files except the anon key (documented). |
| 2 | **Privacy & Data** — vault never reaches our infra. Telemetry (default-on, opt-out): 9 anonymous fields. No PII. EU-residency. 24-month retention. DSAR via delete-my-UUID endpoint. | Rob (Absolution Labs LTD) | **OK** (publish privacy policy pending — operator task) | Supabase project pinned `eu-west-2` (London); RLS restricts anon to INSERT only; CHECK constraints enforce schema; DSAR helper `telemetry/scripts/delete_uuid.sh` tested; privacy policy text complete at `docs/privacy-policy.md` (publish to absolutionlabs.com pending). |
| 3 | **Prompt Injection** — refuse-to-scaffold gate (any files in target dir = refuse) closes blast radius. Intake answers length-capped + character-class-restricted. Phase 2 guide includes "vetting raw/ content" note for ongoing user-side discipline. | Rob (Absolution Labs LTD) | **OK** | Refuse-on-existing-files gate implemented per SKILL.md Step 2; intake input validation applied at SKILL.md Step 3 (length cap + character class); telemetry payload never reaches an LLM (structured JSON to PostgREST, CHECK constraints reject anything off-schema). |
| 4 | **Attack Surface** — public surfaces: telemetry endpoint, install URL (GitHub repo pending), docs site, skill bundle. | Rob (Absolution Labs LTD) | **OK** (custom domain + bundle signing deferred to v1 public ship) | Telemetry endpoint: two-layer Postgres rate-limit (5/60s/UUID + 1000/60s global), RLS-restricted anon insert-only, CHECK constraints reject malformed payloads; install URL via GitHub HTTPS; manifest lint (`scripts/lint_manifest.py`) runs 10/10 pre-publish. Custom domain `telemetry.absolutionlabs.com` + bundle signing + SHA256 checksums deferred to public ship (Chunk 7). |
| 5 | **Backup & Rollback** — user's vault: cloud sync provides versioning. Our infra: Supabase Pro PITR + daily snapshots. Skill bundle: every version in git; version-pinned install URLs via GitHub tags. | Rob (Absolution Labs LTD) | **OK** (Recovery Drill execution pending — operator task) | Recovery Drill procedure documented at `release/recovery-drill.md`; Supabase Pro PITR confirmed active; rollback procedure documented in `docs/upgrading.md`. First drill execution pending. |
| 6 | **External Platform Config** (OP #11) — Obsidian, agent CLIs, Dataview, cloud sync providers, Supabase platform-level controls. No admin relationship with most. Loud-when-broken is best we can do. | Rob (Absolution Labs LTD) | **Accepted risk** (with quarterly SUSTAIN cadence) | COMPATIBILITY.md states tested versions; quarterly refresh process documented; quarterly Supabase SUSTAIN check (account standing, Pro plan, region, HSTS, DPA) documented at `telemetry/README.md` § Quarterly SUSTAIN check. |

---

## Key Decisions

| # | Decision | Rationale | Reversibility | Date |
|---|---|---|---|---|
| 1 | Distribution = shareable direct URL only; not marketplace-listed | Sales-tool framing — we control who sees it | Easy | 2026-06-02 |
| 2 | Full Absolution Labs LTD branding throughout | Trust artifact must visibly bear the brand it's anchoring | Easy | 2026-06-02 |
| 3 | Free skill, no upsell embed | Trust artifact, not a funnel | Easy | 2026-06-02 |
| 4 | Single-company vault shape only, permanently | Cuts complexity ~40%; multi-client isn't the use case | Easy | 2026-06-02 |
| 5 | Manual lint only, no scheduler | Avoids per-OS scheduler engineering + sleep/wake failure mode | Easy | 2026-06-02 |
| 6 | Drop shipped plugin bundle; user installs Dataview via Obsidian browser | Cuts plugin-version maintenance + supply-chain risk | Easy | 2026-06-02 |
| 7 | Drop Templater entirely; ship in-vault markdown templates | Confirmed not used in our own workflow | Easy | 2026-06-02 |
| 8 | Cut first-project scaffolding; hand off to `new-project-setup` next session | Cleaner separation: this skill sets up vault, next session starts work | Easy | 2026-06-02 |
| 9 | Cut git init; cloud sync handles versioning | Non-technical users don't use git | Easy | 2026-06-02 |
| 10 | Phase 1 (skill) / Phase 2 (living guide) split | Sharp install moment + honest naming of ongoing-use docs | Easy | 2026-06-02 |
| 11 | License = MIT | Standard, permissive, well-understood; legal warranty disclaim built in | Easy | 2026-06-02 |
| 12 | All disclaimers reference "Absolution Labs LTD" (full legal name); informal copy may use "Absolution Labs" | Operator directive 2026-06-02 | Easy | 2026-06-02 |
| 13 | Telemetry = default-on, opt-out (not opt-in) | Sales-tool framing demands we detect failure before prospect tells anyone | Hard | 2026-06-02 |
| 14 | Refuse-to-scaffold if target dir has files or `.obsidian/` exists | Prevents data damage; closes prompt-injection blast radius too | Easy | 2026-06-02 |
| 15 | First-screen compliance gate (3 mandatory checkboxes) | Regulated-sector prospects need to surface compliance concerns BEFORE writing data | Easy | 2026-06-02 |
| 16 | Direct support email, 1-business-day SLA | Branded skill in sales-tool frame demands branded support | Hard | 2026-06-02 |
| 17 | Appetite escalated Small → Big | Pre-mortem reframe (sales/trust tool) requires polish + test rigor that triples build effort | Hard | 2026-06-02 |
| 18 | Sales-tool framing, not adoption-volume framing | Per-install quality is absolute; volume doesn't matter | Permanent | 2026-06-02 |

## Session Log

| # | Date | What happened | Open items |
|---|------|---|---|
| 1 | 2026-06-02 | SHAPE conversation: Collaboration Mode, Working Backwards, Scope, 6 Assumptions, Pre-Mortem (reframed mid-session from adoption-funnel to sales/trust tool), Threat & Recovery Map (6 surfaces). 18 decisions logged. Gate 1 PASS + Gate 1.5 PASS (conditional on evidence-landing during BUILD). Companion docs updated: [shape-1-scope.md](shape-1-scope.md) §17 reframe addendum; [current-setup-user-guide.md](current-setup-user-guide.md) §9 open items resolved. Chunk 2 BUILD complete: 16 template files + folder structure under `templates/` (SCHEMA.md, CONTEXT.md, index.md, log.md, HOW-TO-USE-THIS.md, claude-operating-principles.md, expectations.yml, 3 page templates, 5 .obsidian configs, CLAUDE.md + AGENTS.md project stubs). | Chunks 3-7 + new Chunks 5b (telemetry endpoint) / 6.5 (demo video) / J/K/L (per §17). Suggested next chunk: 3 (SKILL.md procedure) using Chunk 2 templates as the content layer. |
| 2 | 2026-06-02 | Chunk 3 BUILD complete: [SKILL.md](SKILL.md) authored at project root (~430 lines). Procedure wraps Chunk 2 templates with: pre-flight (worktree-refusal per OP #19 + mount/writability + skill-bundle integrity), Step 1 compliance gate (3 mandatory checkboxes per decision #15), Step 2 refuse-to-scaffold gate (decision #14), Step 3 2-question intake (company + sync provider) with input validation, Step 4 date-format auto-detect from locale, Step 5 telemetry display + opt-out (default-on per decision #13), Step 6 idempotent scaffold (substitution table, .obsidian configs, schema/context/index/log, concepts page, _meta, project-stub `.template` files preserved for `new-project-setup`), Step 7 round-trip test (`entities/test-welcome.md` + index update + 5-step user verification), Step 8 HOW-TO-USE-THIS handoff, Step 9 final message + success-ping + continuation prompt. Surface-difference table (Cowork vs Code), failure-modes section (mid-scaffold / round-trip / telemetry / slow writes), idempotency contract, out-of-scope table mapped to key decisions. Stale shape-1-scope §5 pseudocode left as historical context (it predates the reframe; brief is canonical). | Chunk 4 (manifests: `plugin.json` Cowork + Claude Code skill frontmatter mirror), Chunk 5 (lint procedure as separate skill/script), Chunk 5b (CF Worker telemetry endpoint + D1 + privacy policy at absolutionlabs.com), Chunk 6 (README + LICENSE + COMPATIBILITY.md + docs/{customisation,troubleshooting,upgrading}.md), Chunk 6.5 (3-min demo video), Chunk 7 (test matrix execution across 7 personas + public install URL + Recovery Drill). Suggested next chunk: 4 (manifests — small, ~10 min — unblocks both surfaces for testing). |
| 3 | 2026-06-02 | Chunk 4 BUILD complete: [plugin.json](plugin.json) (Cowork plugin manifest, ~125 lines) + [MANIFESTS.md](MANIFESTS.md) (mirror-contract doc, ~95 lines). plugin.json captures: identity, version, license, publisher block (legal-name discipline per Decision #12), surfaces=[cowork], permissions block (directory_access required + scoped to vault writes + reads gated on refuse-to-scaffold; network_egress optional + opt-out-blocked + EU-residency), telemetry block (mirrors SKILL.md Step 5 — opt-out at runtime, 8 payload fields, no PII, 24-month retention, DSAR via UUID), gates block (worktree refusal / compliance checkbox / refuse-to-scaffold / bundle integrity — each pointing at the SKILL.md step that owns it), files manifest (24 paths the Cowork bundle ships), trigger phrases + URL paste, time-to-run breakdown, checksums + signing URLs (Chunk 7 release process). SKILL.md frontmatter serves as the Code-side manifest; no separate file needed for Code. MANIFESTS.md documents the mirror contract: 6 fields that must stay in sync between the two manifests, surface-only fields and why, what changes between v1.x releases, an 8-step pre-release manifest lint candidate (Chunk 5). shape-1-scope §5 + §13 marked superseded via top-of-doc addendum pointing readers at SKILL.md + brief.md as canonical. | Chunk 5 (lint procedure — separate skill or `scripts/lint_vault.py` + manifest-lint script per MANIFESTS.md §"Pre-release manifest lint"), Chunk 5b (CF Worker telemetry endpoint + D1 + privacy policy at absolutionlabs.com — Threat-Map surfaces #1 + #2 + #4), Chunk 6 (README with dual install picker + LICENSE + COMPATIBILITY.md + docs/{customisation,troubleshooting,upgrading}.md), Chunk 6.5 (3-min demo video), Chunk 7 (test matrix execution across 7 personas + public install URL + Recovery Drill). Suggested next chunk: 5b (telemetry endpoint) — blocking for Threat-Map surfaces #1/#2/#4 evidence, and without it the SKILL.md Step 5 telemetry pings have nowhere to land. Alt: Chunk 6 (README + LICENSE) if Rob wants the public-facing install surface drafted before telemetry infra. |
| 4 | 2026-06-02 | Chunk 6.5 BUILD partial (script only; Loom recording itself is Rob deliverable post-5b + post-6): [loom-script.md](loom-script.md) authored (~290 lines). Target length 6-8 min (longer than the brief's original 3-min daily-flow framing because Rob reframed mid-session as "guided install, full of rich context and insights" — different artifact). Structure: pre-recording prep checklist (8 items), director's tone notes, 10 scenes with on-screen action + verbatim narration + per-scene director notes, three explicit "rich context" trust beats (Scene 3 deliberate-cuts, Scene 9 Phase 1/2 handoff, Scene 10 publisher CTA), failure-mode rule (uncut failures look honest), post-recording checklist (Loom title / description / thumbnail / embed / cross-post). Caveats section notes: don't record until absolutionlabs.com URLs are live (Chunks 5b + 6) or expect to re-record URL-bearing segments. Also enumerated: additional website surfaces beyond bundle-level docs (install landing page, privacy page, feedback form, embedded Loom) — added to Deliverables table. Pre-distribution testing path flagged as Chunk 7 test-matrix extension (private beta URL → 2-3 friendly prospects → public URL). | Chunks 5 (lint procedure), 5b (CF Worker telemetry + D1 + privacy page), 6 (bundle-level docs: README + LICENSE + COMPATIBILITY + customisation/troubleshooting/upgrading), 6b NEW (website-level surfaces: install landing page copy + Loom embed + feedback form spec), 7 (test matrix + private beta gate + public URL). |
| 5 | 2026-06-02 | Chunk 5b BUILD complete (code + docs; deploy + legal-review pending and operator-owned). Files landed: [telemetry/worker.ts](telemetry/worker.ts) (~165 lines TypeScript CF Worker — payload schema validator with 8-field allowlist + format regexes [UUID v4, semver, ISO timestamp, enum-checked OS/surface/outcome/sync_provider], rate-limit binding [5/min/IP], 2KB body cap, HSTS + nosniff + CSP headers, two endpoints `POST /v1/install` + `GET /v1/health`, no public DELETE endpoint by design — DSAR is human-mediated to avoid DOS), [telemetry/schema.sql](telemetry/schema.sql) (install_events + dsar_log tables, CHECK constraints on enums, 4 indexes, ISO timestamp defaults), [telemetry/wrangler.toml](telemetry/wrangler.toml) (deploy config with `<CF_ACCOUNT_ID>` / `<D1_DATABASE_ID>` / `<RATE_LIMIT_NAMESPACE_ID>` placeholders + EU region note `--location=weur` + `workers_dev = false` + 50ms CPU cap), [telemetry/package.json](telemetry/package.json) (npm scripts: deploy / dev / db:apply:remote / db:prune / dsar:delete), [telemetry/tsconfig.json](telemetry/tsconfig.json), [telemetry/scripts/delete_uuid.sh](telemetry/scripts/delete_uuid.sh) (DSAR helper — UUID format validation + confirm prompt + atomic delete-and-audit-insert + suggested reply text), [telemetry/README.md](telemetry/README.md) (~150 lines — initial-deploy 9 steps, smoke tests, weekly funnel-review query, monthly prune, quarterly SUSTAIN audit per OP #11 surface #6, DSAR procedure, Threat-Map row-by-row mapping), [telemetry/.dev.vars.example](telemetry/.dev.vars.example) + [telemetry/.gitignore](telemetry/.gitignore), [docs/privacy-policy.md](docs/privacy-policy.md) (~170 lines — covers only the install-telemetry surface, not the wider Abso Labs privacy posture; lawful basis = legitimate interests with balancing-test assessment, 9-field disclosure table verbatim against the schema, 24-month retention, EU residency, UK GDPR DSAR rights, ICO complaint route, 8-item internal pre-publication checklist incl. UK GDPR lawyer review). Threat-Map alignment: surface #1 (secrets — no secrets in payload, CF token in 1P), surface #2 (privacy — EU pinning, schema enforcement, DSAR via UUID), surface #3 (prompt injection — payload never reaches an LLM, char-class restricted), surface #4 (attack — rate limit + body cap + HSTS + custom-domain only + tight CPU cap), surface #5 (rollback via `wrangler rollback` + D1 daily snapshots), surface #6 (quarterly SUSTAIN audit in telemetry/README.md). | Chunk 5b deploy: Rob runs `wrangler d1 create … --location=weur`, fills 3 placeholders in wrangler.toml, applies schema, deploys Worker, maps `telemetry.absolutionlabs.com`. Chunk 5b legal: privacy policy reviewed by UK GDPR lawyer before publish. Chunks 5 (lint), 6 (bundle docs), 6b (website surfaces), 6.5 Loom recording (post-5b deploy + post-6 landing page), 7 (test matrix + private beta + public URL). Suggested next: Chunk 6 (README + LICENSE + COMPATIBILITY + docs/customisation,troubleshooting,upgrading) — public-facing surface, ~45 min, unblocks website mockups while Rob handles 5b deploy + legal review async. |
| 6 | 2026-06-02 | Chunk 6 BUILD complete: bundle-level docs landed. [LICENSE](LICENSE) (MIT, © Absolution Labs LTD 2026). [README.md](README.md) (~115 lines — hero block, dual install picker [Cowork URL + Code one-liner], version-pinning callout, Loom embed placeholder, "Before you install" prerequisites + compliance gate preview, "What gets installed" file-tree, "What the skill does NOT install" with key-decision references, three-layer architecture paragraph, privacy + telemetry summary, security & integrity [bundle signing, checksums, threat model link], compatibility link, full doc index, support/feedback/license/about blocks, MIT-license fork-permission line + "rename your variant" guidance). [COMPATIBILITY.md](COMPATIBILITY.md) (~125 lines — tested matrix: OSes [macOS 12-15 verified, Windows 10/11 verified, Ubuntu 22/24 verified, Fedora/Debian/ChromeOS untested], agent CLIs [Cowork + Code primary, Codex + opencode compatible-not-first-class], Obsidian [≥1.5], community plugins [Dataview strongly recommended, Templater not recommended per decision #7], cloud sync providers [9 providers; Dropbox-for-Business / OneDrive-for-Business / Google Drive carry explicit caveats; MDM-managed Windows in test pending Chunk 7], locales [7 mappings + ISO fallback], "what 'not compatible' looks like" failure surface, quarterly refresh cadence). [docs/customisation.md](docs/customisation.md) (~170 lines — fork-your-own guide: safe-to-edit content layer, edit-with-care procedure layer, generally-leave-alone telemetry layer; 8 common customisations [rebrand / change starter principles / add intake question / add wiki folder / change lint thresholds / swap .obsidian defaults / replace telemetry (3 sub-options) / change pricing model]; re-packaging checklist; "what we'd like to know" + "what we'll never do to your fork" trust block). [docs/troubleshooting.md](docs/troubleshooting.md) (~175 lines — pre-install issues [Cowork URL paste / Code script trust / compliance checkbox refusal], install-time pre-flight refusals [non-empty target / worktree detection / bundle integrity], scaffold failures [mid-scaffold / pathological slow writes / no-write-access], round-trip verification failures [5 specific causes], runtime issues [telemetry unreachable / DSAR mechanic / refresh question / broken wikilinks / SCHEMA drift / start-over], cloud sync conflict files [4 providers with native conflict patterns], lost/damaged vault recovery [4 levels in order], escalation path with explicit info-to-include checklist). [docs/upgrading.md](docs/upgrading.md) (~150 lines — the two meanings of "upgrade" disambiguated, version-pinning rationale, how-to-know-your-version, Cowork + Code upgrade flows, refreshing-an-existing-vault as manual op-with-workflow, when-NOT-to-refresh, rolling back skill artifacts vs rolling back vault, version retention policy [URLs alive indefinitely, backport policy for last-two-minor, withdrawal protocol], release cadence [event-driven not scheduled], how-to-discover-new-versions [no auto-update, no phone-home, intentional]). Total ~735 lines across 5 new files + LICENSE. Cross-links bound: README → all docs; troubleshooting → upgrading + HOW-TO-USE-THIS; upgrading → troubleshooting + customisation; customisation → MANIFESTS. | Chunks 5 (vault + manifest lint scripts per MANIFESTS.md §"Pre-release manifest lint"), 6b (website surfaces: install landing page copy + Loom embed + feedback form spec + privacy page integration), 7 (test matrix across 7 personas + private beta gate + public install URL + Recovery Drill), 5b operator-side (CF deploy + UK GDPR lawyer review), 6.5 operator-side (Loom recording itself — blocked on 5b deploy + 6b landing page since the script references URLs that won't resolve until both ship). Suggested next: Chunk 6b (website surfaces — small, ~30 min, drafts the install landing page copy that the README links to and gives Rob a working surface to mock up while async deploy + legal review run). |
| 8 | 2026-06-03 | Chunk 5b RE-IMPLEMENTED on Supabase (Option B — direct PostgREST + RLS) and LIVE. Rob picked Supabase over Cloudflare after wiki check confirmed established Abso Labs Supabase patterns ([[concepts/edge-function-as-attio-writer-pattern]], [[concepts/supabase-cli-remote-ddl]], [[concepts/supabase-security-advisor-cleanup]], [[concepts/postgrest-schema-drift-pattern]]). Picked B over A despite schema-drift risk; mitigation = locked-schema convention documented at top of migration file (8 fields frozen for v1, future fields must be nullable with default). All ops via CLI per "yes CLI via 1p" direction; secrets via `op://` per OP #18. **What I did:** (1) Created project `obsidian-company-memory-telemetry` (ref `vujwcvqiwwpncnhgxjsu`) on `eu-west-2` (London — matches AB Asterley convention); DB password generated by 1P at `op://API Keys/Supabase - Obsidian Telemetry - DB password/password`, never touched disk. (2) Wrote + applied [supabase/migrations/20260603000000_install_events.sql](telemetry/supabase/migrations/20260603000000_install_events.sql) — `install_events` table (UUID + 7 fields + auto created_at), `dsar_log` table (UUID + timestamp + audit), RLS enabled on both, anon-INSERT-only policy on install_events, CHECK constraints on every column (regex for UUID/semver/ISO-ts; enum for os/surface/outcome/sync_provider; len ≤64 for failure_step), `enforce_install_rate_limit()` trigger (5/60s/UUID — required SECURITY DEFINER fix mid-build because SECURITY INVOKER let anon see 0 rows via RLS and the limit never fired), `dsar_delete_uuid()` SECURITY DEFINER function (atomic DELETE + audit INSERT), `prune_old_install_events()` SECURITY DEFINER function (24-month retention), explicit REVOKE anon + authenticated on the three functions per [[concepts/supabase-security-advisor-cleanup]] "REVOKE FROM PUBLIC trap". (3) Captured service-role key at `op://API Keys/Supabase - Obsidian Telemetry - service role/credential`. (4) Smoke-tested 4 paths LIVE: anon INSERT → 201 ✓; anon SELECT → 200 + `[]` (RLS blocks read) ✓; invalid `os` value → 400 with CHECK constraint message ✓; rate limit → 5×201 then 6×400 with `rate_limit_exceeded` ✓. (5) Updated bundle: SKILL.md Step 5 implementation-note (Supabase + anon-key + RLS + Postgres trigger; replaced CF-Worker text) + Step 9.1 (PostgREST POST URL + 4 headers + same JSON body + 201 expected); plugin.json `telemetry` block (endpoint + provider + region + anon_key + `_anon_key_note` explaining public-by-design + Postgres-rate-limit description); docs/privacy-policy.md (§3 endpoint URL, §3 IP-not-stored language, §5 processor changed Cloudflare → Supabase + AWS sub-processor + DPA, §9 third-party-sharing same swap, §10 security posture rewritten for RLS + CHECK + trigger). (6) Replaced telemetry/README.md entirely (CF runbook → Supabase runbook with project details table, initial-setup script for rebuild, the 4 smoke tests verbatim, daily/weekly/monthly/quarterly ops, DSAR procedure using the SQL function via PostgREST RPC, Threat-Map alignment table). (7) Replaced telemetry/scripts/delete_uuid.sh — wrangler-based → calls `dsar_delete_uuid()` RPC with service-role key from 1P, never touches disk, with reply-text template. (8) Removed CF leftovers: `worker.ts`, `wrangler.toml`, `package.json`, `tsconfig.json`, `schema.sql`, `.dev.vars.example`. Updated `.gitignore` for `supabase/.temp/`. (9) E2E test with bundle config: read endpoint + anon_key from plugin.json directly, POST exact SKILL.md Step 9.1 shape → 201 + row landed + verifiable via service role. (10) Cleaned all smoke-test rows; install_events now empty (count 0). **Decisions worth flagging:** (i) Schema-drift class bounded by the lock not eliminated; future schema evolution requires new skill version + coexistence period. (ii) Endpoint is the raw Supabase URL `vujwcvqiwwpncnhgxjsu.supabase.co` — works for testers tomorrow; custom domain `telemetry.absolutionlabs.com` deferred until v1 public ship. (iii) Service-role key in `API Keys` 1P vault; rotation candidate every 12 months. (iv) DPA with Supabase still needs to be filed (operator task). (v) Privacy policy text now references Supabase + AWS sub-processor + London region — Rob deferred legal review, but the disclosure table matches the schema exactly. (vi) MANIFESTS.md not yet updated to reflect new `anon_key` field — should add to the surface-only-in-plugin.json section. | Chunks 5 (lint), 6b (website surfaces), 7 (test matrix + ship + Recovery Drill), Loom recording (NOW UNBLOCKED — telemetry endpoint is live; only blocker remaining for Scene 6 is Rob saying "privacy policy lives on our website" while it's not yet published. Either delay Loom recording until privacy policy is at absolutionlabs.com/privacy OR re-record Scene 6 later. The endpoint itself is no longer a blocker.). Operator follow-ups: (a) sign DPA with Supabase, (b) file the dpa-template completion record, (c) consider custom domain telemetry.absolutionlabs.com for v1 ship, (d) MANIFESTS.md update for anon_key field. Suggested next: Chunk 6b (website surfaces) — the privacy page is the remaining blocker for the Loom; the install landing page would let Scene 1 record cleanly too. ~30 min of website-copy authoring. |
| 9 | 2026-06-03 | Closing-out-the-bundle session. Rob said "you complete everything on your end tonight, i will do mine in the morning" — autonomous build with no further checkpoints. Six artefact clusters landed: **(1)** [TESTERS.md](TESTERS.md) at bundle root (~170 lines, tester-facing — pre-install checklist, install steps for Cowork + Code, what-to-see-during-install moments mapped to SKILL.md steps, what-to-do-once-installed 5-step exercise, 9-question feedback frame, telemetry + privacy plain-English aside, WhatsApp fallback line, "what we'll do with your feedback" trust block). **(2)** MANIFESTS.md updated — added `telemetry.endpoint` + `telemetry.anon_key` + `telemetry.endpoint_provider` + `telemetry.endpoint_region` row to surface-only-in-plugin.json table (anon-key public-by-design noted); pre-release lint expanded from 8 → 11 checks (endpoint reachability, JWT decode for anon_key, network_egress vs telemetry endpoint match); reference now points at `scripts/lint_manifest.py`. **(3)** Chunk 6b website surfaces — new [website/](website/) folder: [README.md](website/README.md) (bundle-vs-website split, deploy order, anti-deploy rules), [install-page.md](website/install-page.md) (~115 lines — 8-section structural spec: hero with dual install buttons, Loom block, 3-card "what it is", file-tree, "what it does NOT install", privacy summary, compatibility tiles, support footer; style + mobile + analytics rules; "when to revise" cadence), [privacy-page-deploy.md](website/privacy-page-deploy.md) (Model A "one privacy URL covers everything" recommended over Model B "per-product URL"; render step for Netlify/Hugo/Astro; pre-publish checklist; post-publish smoke-test; revision triggers), [feedback-form-spec.md](website/feedback-form-spec.md) (v1 decision = email-only, no form; documents minimum-viable form schema + RLS pattern if/when needed; revisit at month 3), [loom-embed-snippet.md](website/loom-embed-snippet.md) (16:9 iframe wrapper with lazy-load + Loom `hide_*` params for clean trust artifact; fallback for pre-recording state; version-pinning convention; alternative-player rules). **(4)** Chunk 5 lint — [scripts/lint_manifest.py](scripts/lint_manifest.py) (~230 lines Python implementing the 11-check pre-release manifest lint from MANIFESTS.md; bash version superseded — JSON parsing was brittle without jq, Python with json+yaml is cleaner). Currently RUNS GREEN against live bundle: 10/10 PASS (the 11 checks include the live HTTP probe to the Supabase endpoint, JWT decode, RLS-policy-via-anon-key-content verification, files-exist check, basenames-parity between plugin.json.writes and SKILL.md tokens). UTF-8 stdout reconfig added for Windows console safety. **(5)** Chunk 7 release procedures — new [release/](release/) folder: [test-matrix.md](release/test-matrix.md) (~200 lines — 7 personas with Setup / Install procedure / Pass criteria columns + Why-it-matters block + persona run record template + gate logic + ~5h total time estimate; Persona 6 MDM is the only "clean error counts as pass" exception), [private-beta-gate.md](release/private-beta-gate.md) (5-stage process: prerequisites → invite 3-5 from candid relationships → 7-day window → triage into 3 buckets [ship-blocking / address-before-ship / public-launch-nice-to-have] → decision held 2-3 days after last reply; cadence post-ship 2-week-tight then weekly), [recovery-drill.md](release/recovery-drill.md) (9-step drill: seed live with 3 marker UUIDs → trigger Supabase backup → restore via PITR Method A or SQL-dump Method B → verify schema [RLS + policies + constraints] → verify data → verify constraints with 4 smoke tests → paper-exercise bundle config swap → clean up → log row; failure-modes map to specific remediations; drill log table awaiting first run). **(6)** Companion docs — current-setup-user-guide.md status note added at top pointing readers at the 4 canonical user-facing surfaces (README, HOW-TO-USE-THIS, TESTERS, install-page) rather than rewriting body; shape-1-scope.md already has §17 reframe addendum + §5/§13 supersession header from earlier session. **Things that came up that I solved myself:** (a) Python3 unavailable as `python3` on Windows but `python` is — used that. (b) Bash JSON parsing for the lint was brittle (description-with-commas broke the sed); rewrote in Python — runs much cleaner. (c) Writes-parity lint check needed widening from "Step 6 only" to "whole SKILL.md body" because the round-trip welcome page is legitimately created in Step 7. (d) Windows console cp1252 codec choked on Unicode arrows in lint output; added `sys.stdout.reconfigure(encoding="utf-8")`. **Brief sweep at end of session** (this row + status line update + Deliverables table marks 5 + 6b + 6.5-script + 7 as Complete). | Operator-only items remaining: (i) `git init` + push to public GitHub for tomorrow's testers, (ii) confirm `info@absolutionlabs.com` mailbox monitored, (iii) execute test matrix against 7 personas, (iv) run Recovery Drill, (v) sign Supabase DPA + file the dpa-template completion record, (vi) deploy privacy policy to absolutionlabs.com/privacy (Netlify), (vii) deploy install landing page to absolutionlabs.com/obsidian (Netlify), (viii) record the Loom (blocked only on privacy URL being live), (ix) custom domain `telemetry.absolutionlabs.com` → Supabase project (deferred). My side: no remaining build work for v1 ship. |
| 10 | 2026-06-03 | Adversarial fix-pass. Rob asked to spin up sub-agents to challenge the bundle before publishing. Three parallel general-purpose agents (security / functionality / cross-file drift) returned 5 BLOCK + 12 HIGH + 11 MEDIUM + 16 LOW findings. Rob said "run all fixes." Worked through every BLOCK + named HIGH. **What landed:** **(Live DB)** Applied two changes to Supabase project `vujwcvqiwwpncnhgxjsu`: (a) tightened `install_events.failure_step` CHECK constraint with `[a-z0-9_:.-]+` regex (closes stored-XSS-in-operator-console class — security MED #4); (b) added second rate-limit trigger `enforce_global_install_rate_limit()` capping 1000 inserts/60s globally to close the UUID-rotation bypass of the per-UUID limit (security HIGH #1 — attacker holding the public anon key could spin random UUIDs and the original trigger always saw count=0). Migration file in repo updated to match live state. **(SKILL.md)** Major rewrites: Step 3 Q2 added explicit user-label-to-kebab-token normalization table for `sync_provider` (was BLOCK Func #1 — user picks "Dropbox" but SQL CHECK wants "dropbox"; every install would have 400'd); Step 4 split into 4a (resolve `os` per surface — Python on Code, ask-user on Cowork — Func HIGH #6) + 4b (date format); Step 5 telemetry surface rewritten to declare "nine fields" verbatim with `failure_step` + UTC timestamp called out (was BLOCK Drift #2 — 8-vs-9 contradiction across 6 files); Step 6 substitution table got "Substitution scope exceptions" subsection documenting the three carve-outs explicitly (Func HIGH #7 + LOW #13); Substep 6.5 rewritten to substitute `{{COMPANY_NAME}}` in per-page templates but NOT `{{TODAY}}` (was BLOCK Func #2 — baking scaffold date into user-copy templates defeats lint stale-page check); Substep 6.7 scaffold-version.txt notation clarified as runtime-built not template-substituted (Func LOW #14); Step 7 restructured — Substep 7.3 now just verifies the initial entry, NEW Substep 7.5 appends round-trip result to `log.md` AFTER user confirmation (was Func HIGH #4 — old `log.md` template pre-asserted success before round-trip ran, corrupting audit trail on first failure); Step 9.1 body shape clarified with normalized `os` + `sync_provider` references, `failure_step` shown as failed-path-only second JSON block with explicit charclass, "only outcome differs" sentence replaced with accurate "failure_step is the only field that differs" (Func HIGH #5 / Drift #2); metadata footer "5 fields" → "9 fields" (Drift HIGH #6); telemetry implementation note rewritten for two-trigger rate limit (HIGH #1). **(plugin.json)** Added `failure_step` to `payload_fields` (now 9); added `_payload_fields_note` explaining mandatory-vs-failure-only; `rate_limit` description now reflects both triggers. **(templates)** `log.md` initial entry stripped of pre-asserted "round-trip passed" text; `community-plugins.json` changed from `["dataview"]` to `[]` (Func MED #8 — contradicted Decision #6 "no plugin bundles"). **(docs)** `README.md` Privacy section rewritten Cloudflare → Supabase + 9-field disclosure (BLOCK Drift #1); README signing/checksums claim softened to "will ship at public release" (HIGH Drift #5); `customisation.md` § telemetry rewritten — `worker.ts`/`wrangler.toml` references replaced with Supabase migration + anon-key path (BLOCK Drift #3); `customisation.md` "Eight checks" → "Eleven checks" + path to `lint_manifest.py`; `troubleshooting.md` `telemetry.absolutionlabs.com` → live Supabase URL with custom-domain-planned note (HIGH Drift #4); `troubleshooting.md` "instead of support@" → "instead of info@" (MED Drift #9); `privacy-policy.md` ts example standardized to 2026-06-03; `install-walkthrough.md` Step 3, 4, 5, 7 sections updated to mirror the SKILL.md changes (sync_provider normalization, os capture, 9-field telemetry text, Substep 7.5 reference). **(brief.md)** Threat & Recovery Map flipped surfaces 1-5 from `**Open**` to `**OK**` with evidence pointers to actual implementations (HIGH Drift #7); surface 6 stays `Accepted risk` with quarterly SUSTAIN cadence pointer. **(TESTERS.md)** "nine steps" → "seven user-visible steps (plus pre-flight checks)"; "eight anonymous fields" → "nine anonymous fields"; **(telemetry/README.md)** "8-field" → "9-field" in Threat-Map row 3. **(scripts/lint_manifest.py)** Check 10 hardened: now validates `iss=supabase` + `exp` is in the future + `exp` is at least 30 days out + extracts `ref`; new Check 12 binds `endpoint` hostname to JWT `ref` (was HIGH Security #3 — original lint would have passed a malicious-fork manifest that swapped both endpoint + anon key to attacker infra). Lint now 11/11 PASS (was 10/10 before — the new Check 12 binds endpoint↔ref). **(telemetry/scripts/delete_uuid.sh)** Added `trap 'SVC_KEY=""; unset SVC_KEY' EXIT INT TERM` to clear service-role key on any exit path including Ctrl-C — defence-in-depth per OP #18 against /proc/<pid>/environ leak window during the `op read` → `curl` interval (was MED Security #5). **(End-to-end verification)** Smoke-tested live: (a) anon INSERT with `failure_step:"round_trip"` returns 201, (b) anon INSERT with `failure_step:"BAD<script>"` returns 400 with check_failure_step_check message — new charclass works, (c) anon INSERT without `failure_step` (success path) returns 201, (d) install_events cleaned to empty rows. Final manifest lint 11/11. **(Findings deliberately deferred)** Security LOW #6-9 (COMPANY_NAME validation, DSAR-log retention note, Step 2 TOCTOU theoretical, worktree-check pattern looseness); Functionality LOW #15 (SCHEMA.md frontmatter); Drift MED #8 (Session Log row reordering — cosmetic only, dates are clear); Drift MED #11 (10/10-vs-11/11 in brief Session 9 row — superseded by this row). **Sub-agent IDs for further questioning if needed:** security `ae8e39a603dc4f3d2`, functionality `aad7a3fb7e25f6190`, drift `a45eca19b03ebfc32`. | Outstanding (all operator-side, none blocking my work): GitHub repo push (next step Rob authorizes); test matrix execution; Recovery Drill execution; DPA signing; privacy + install pages deployed to absolutionlabs.com; Loom recording. **Verdict change: bundle is now ready to publish** — pending the self-install pass that's still recommended as Level 0 gate per the earlier "ready to publish?" answer. |
| 11 | 2026-06-03 | Resolved the external-skill-dependency gap surfaced during install testing. Rob noticed: the bundle's templates referenced `new-project-setup` and `/close-full` as if they were external skills the user had. For Rob those exist (his user-global skills); for testers they don't. Rob proposed shipping the PROMPTS inside the install and instructing users to create their own skills — chosen over the alternatives (documenting the dependency / rewriting around it / bundling our personal skills) because it (a) makes the bundle self-contained, (b) doesn't expose our personal configs, (c) educates the user about their own AI tool, (d) matches the "control system, not template" framing exactly. **Files added:** [templates/_meta/skill-prompts/README.md](templates/_meta/skill-prompts/README.md) (overview + design rationale + how to install per surface), [templates/_meta/skill-prompts/new-project-setup.md](templates/_meta/skill-prompts/new-project-setup.md) (~210 lines — install instructions for Cowork/Code/Codex/opencode + a generic skill body with 7 steps: pre-flight, collect inputs, derive slug, create folders, write session stub, optional brief, update vault log, hand off; idempotency clause refuses on existing folders), [templates/_meta/skill-prompts/close-session.md](templates/_meta/skill-prompts/close-session.md) (~200 lines — same surface-by-surface install + 6-step body: pre-flight, update brief, verify Ingest, append log, report KB writes, generate continuation prompt, close report; notes that this is the minimum-viable close — Channel A-E drains + cross-vault sync are user-customisation territory documented at the bottom). **Files updated:** SKILL.md (Substep 6.1 added `_meta/skill-prompts/` to folder list; new Substep 6.6 writes the three prompt files with surgical substitution scope: COMPANY_NAME + VAULT_ABSOLUTE_PATH in the body, TODAY in the README only, leave PROJECT_NAME/PROJECT_DESCRIPTION literal for use-time substitution; renumbered old 6.6→6.7, 6.7→6.8, 6.8→6.9; substitution-scope exceptions section gained the skill-prompts carve-out; Step 9.2 final message "What to do next" rewritten — "invoke new-project-setup" replaced with "create two custom skills using prompts at _meta/skill-prompts/"); plugin.json (added 3 skill-prompts paths to `permissions.directory_access.writes` + `files`); templates/HOW-TO-USE-THIS.md (2 close-full references swapped to "your close-session skill", new section "The two custom skills you'll build" added with 2-min overview pointing at the prompt files); templates/SCHEMA.md (§5 close procedure line: "Every session ends with `/close`" → "Every session ends with the `close-session` skill (the user installs this from _meta/skill-prompts/close-session.md)"); README.md ("a separate skill called new-project-setup" → "the install ships prompt files for two custom skills you'll build" + file-tree updated to show `_meta/skill-prompts/`); docs/install-walkthrough.md (Step 9 final message mirrors SKILL.md change); loom-script.md Scene 9 reframed — replaces "run a separate skill called new-project-setup" with the new beat about the install dropping prompt files, with the strong differentiation line "We're not handing you a black box. We're handing you the operating manual and letting you wire it into your tool, your way." Final manifest lint: 11/11 PASS. Total new files: 3. Total file edits: 7. | Operator-only: re-install in Cowork to pick up the new files (raw GitHub URL still resolves to latest commit); test that the `_meta/skill-prompts/` folder lands; optionally follow the prompts in those files to actually build the skills in your Cowork instance and verify they work. After that, the bundle is genuinely self-contained for v1 ship. |
| 16 | 2026-06-03 | Three artefacts in response to operator ask, plus disclaimer wire-up across the GH-hosted user-facing docs per the wiki research from this session. (1) [website/overview-page.md](website/overview-page.md) NEW — canonical website surface for `absolutionlabs.com/obsidian`. Story-led 10-section structure: hero (no install buttons above fold) → Loom embed → What it is (250w in AL tone) → How we built it (Forge methodology + 14 sessions + threat-map references) → What it was based on (AL CKB + research swarm + Operating Principles) → How it helps us (trust artefact + lowers cost of paid engagements) → Who it's for (two-column qualifier pulled from standard-vs-ours.md) → Get the skill (single "Get it on GitHub →" CTA, only install CTA on the page) → Privacy summary → About + Companies Act footer. Why-this-shape section explains the deliberate reversal of install-page.md's button-led approach: regulated-sector founders need to read what we shipped before clicking. (2) [website/install-page.md](website/install-page.md) marked SUPERSEDED with a top-of-doc note pointing readers at overview-page.md; original content preserved per OP #4. (3) [DISCLAIMERS.md](DISCLAIMERS.md) NEW at repo root — canonical disclaimer doc built from today's wiki research at [[concepts/legal-dd-pass-for-free-content]] + [[entities/absolutionlabs-ltd]] § Companies Act compliance. 13 sections + TL;DR: use at own risk / educational purposes only / AI-output verification responsibility / no compliance guarantee / no fitness-for-purpose / third-party tools outside our control / limitation of liability (greater of 12-month fees OR £100 floor per Anthropic pattern) / UK statutory carve-outs (death, personal injury, fraud — non-excludable under UCTA 1977) / indemnity for forks / governing law (English law, English courts exclusive jurisdiction) / changes-policy / notice-serving address / Companies Act 2006 trading-disclosure. Liability cap pattern matches McKinsey's "not engaged in rendering professional advice" + Anthropic's "outputs may contain inaccuracies — independently verify" — both transplantable single clauses identified in the wiki research. (4) Disclaimer wire-up: prominent "Use at your own risk — read this before installing" block added to README.md (between intro and Install section); footer disclaimer block + Companies Act trading-disclosure line appended to README.md, SKILL.md, COMPATIBILITY.md, TESTERS.md, docs/install-walkthrough.md, docs/manual-install-guide.md, docs/manual-install-guide-scope.md, docs/customisation.md, docs/faq.md, docs/standard-vs-ours.md, docs/troubleshooting.md, docs/upgrading.md, docs/privacy-policy.md (13 user-facing docs total). Each footer is tailored to its doc's specific risk surface (e.g. customisation.md mentions fork-indemnity; manual-install-guide.md flags the missing safety checks vs skill path; troubleshooting.md flags recovery-step environmental risk; standard-vs-ours.md disclaims third-party endorsements). README.md doc-list also gained an explicit DISCLAIMERS.md entry tagged "Required reading before install." Operator-internal docs skipped (brief.md, MANIFESTS.md, loom-script.md, release/*, telemetry/*, scripts/*) — not user-touchable so disclaimers wouldn't add value. | Operator: nothing new blocking. /privacy + /obsidian deploys remain Session 9 carry-forwards. New artefact note: when deploying /obsidian, render overview-page.md (NOT install-page.md). When deploying /privacy, render docs/privacy-policy.md verbatim per the existing wiring. DISCLAIMERS.md should NOT be deployed to the website — it's a GH-hosted doc and the website footer link to GitHub is sufficient surface. Wiki note: this session validated the 4-fix shape from [[concepts/legal-dd-pass-for-free-content]] against a second deployment context (skill bundle, not Resources page) — pattern transferred cleanly with one adaptation (no email-gate / no PECR consent surface needed since the skill is GH-hosted not gated). |
| 15 | 2026-06-03 | Loom script audit + rewrite. Walked every scene against current bundle state. Drift findings: Scene 6 telemetry copy survived the CF→Supabase swap of Session 8 by happily neutral wording (script never named the provider); Scene 7 was stale on file-count + missed `_meta/skill-prompts/` listing; Caveats section flagged Chunk 5b as a URL blocker when 5b is actually done. Plus Rob asked to fold the "who should/shouldn't install" lists from [docs/standard-vs-ours.md](docs/standard-vs-ours.md) in as an intro-level qualifier. Implemented: new **Scene 1B "Is this for you?"** between cold-open and architecture (~60s narration, 220 words, camera-on-you qualifier — built for / not built for, four-option list pulled verbatim from standard-vs-ours.md); rich-context-beat count went from 3 → 4 (new 1B + original 3/9/10). Scene 7 updated to mention `_meta/skill-prompts/` ("two prompt files I'll point at in a minute") setting up Scene 9's payoff; bumped "about 20 files" → "about twenty-five." Scene 1 cold-open tightened to bridge into 1B. Trimmed Scene 3 (190→130w), Scene 6 (200→170w), Scene 9 (280→220w) by cutting filler bridges + redundant restatements. Caveats section updated: telemetry endpoint is live, only `/privacy` + `/obsidian` deploys remain as URL blockers; version-hardcoding caveat removed (script doesn't hard-code version anywhere narrated, safe across 1.0.x). "How this script was shaped" section: constraint count went from 3 → 4, with the new constraint ("pre-qualifying viewers is honest, not narrowing") explicitly sourced to today's session. Post-recording Loom description rewritten to match new shape (qualifier scene + custom skills beat + canonical install URL `/obsidian` not generic root). Net: 261 → 280 lines (+19); 60s added then 30s+10s+15s ≈ 55s trimmed, so length-neutral against original 6-8 min target — now 7-8 min. Script is Loom-ready against current bundle. | Loom recording blockers unchanged: `/privacy` page must be live before Scene 6 records cleanly; `/obsidian` page (now overview-page.md per Session 16) live for clean Scene 1 opening. Both Session 9 operator carry-forwards. |
| 14 | 2026-06-03 | Built the manual install guide following the scope from Session 13. [docs/manual-install-guide.md](docs/manual-install-guide.md) landed at **742 lines** — within the scope estimate of 520-770. 14 sections: when-to-use / prerequisites / get-the-bundle (git clone + zip alternative) / self-check compliance / verify empty / **substitution variables (the load-bearing section — 5 placeholders + 4 carve-out rules + per-OS sed/PowerShell recipes)** / mkdir folder structure / **file-by-file copy and substitute (7a-7i covering all 22 destination files, with substitution column per file, organised by carve-out scope)** / round-trip test (manual version — Obsidian 5-check + log.md second-entry-after-success) / telemetry (manual choice with curl POST recipe + UUID generation per OS) / install custom skills / verification checklist (15 ticks incl. "no file shows literal {{VAULT_ABSOLUTE_PATH}} except these") / 7 common manual-install mistakes with specific recovery / upgrading manually-installed vault / when-(not)-to-switch-to-skill. Carve-out rules are written front-and-centre — Rule 1 (`_meta/templates/*.md` keep `{{TODAY}}` literal), Rule 2 (skill-prompts new-project + close-session keep `{{TODAY}}` + project placeholders literal), Rule 3 (skill-prompts README is the exception that substitutes everything), Rule 4 (`.template` files at root substitute NOTHING) — each tied to a specific file in the file-by-file walk so missing them is structurally difficult. [README.md](README.md) Documentation section gained a link to the guide. | Operator: nothing new blocking. Manual-install path is now self-serve. The scope doc's recommendation of "build on demand" was overruled by operator ask — that's fine; the cost was ~30 min wall-clock and the artefact now reduces the support load on `info@absolutionlabs.com` for the audiences identified in the scope. All Session 9 carry-forwards remain (GitHub push, test matrix, Recovery Drill, DPA, privacy + install pages, Loom). |
| 13 | 2026-06-03 | Three doc-surface additions in response to operator ask. (1) [docs/manual-install-guide-scope.md](docs/manual-install-guide-scope.md) — SCOPE doc only (operator asked for scope+estimate, not the full guide). 15-section TOC mapped to estimated lines per section, total ~520-770 lines / ~3,500-5,500 words / ~15-25 min read; identifies five audiences (Codex/opencode users, trust-conscious / regulated, blocked-egress, forkers, recovery operators); six risks-and-gotchas list anchored on SKILL.md Step 6 substitution-scope carve-outs; build-vs-defer decision aids; recommendation = build on demand post-beta rather than pre-emptively, to avoid premature doc-bloat. (2) [docs/troubleshooting.md](docs/troubleshooting.md) extended with three new sections appended before Escalation: "Bundle download issues" (Cowork URL paste delay, Code skill not visible, git clone failures, zip extract nesting, integrity-check case-sensitivity), "File and folder structure confusions" (.obsidian hidden, .template files invisible in Obsidian, workspace.json noise, vault rename + wikilinks, duplicate vault folders), "Incompatibilities not already covered in COMPATIBILITY.md" (macOS Gatekeeper, Windows SmartScreen, corporate proxy blocking telemetry endpoint, Obsidian plugin browser blocked, Linux glibc / electron, iCloud / Dropbox optimised-storage eviction, locale-driven date format mismatch, Defender for Cloud Apps, MDM dot-folder block). (3) [docs/faq.md](docs/faq.md) NEW — question-shape help orthogonal to troubleshooting's problem-shape; 7 sections (general / vault lifecycle / data and privacy / customisation / AI session behaviour / Obsidian-specific / lint and quality / practical scenarios); ~85 questions answered. (4) [docs/standard-vs-ours.md](docs/standard-vs-ours.md) NEW — diff between four common Obsidian+AI patterns (plain notes + AI, Smart Connections, Templater + scripts, Code/Cursor + vault) and what this bundle ships; 20-row side-by-side comparison matrix; explicit "you should NOT install if" + "you SHOULD install if" lists; migration paths in both directions; "what 'best' depends on" decision aid; open question on extracting the pattern as a general spec. (5) [README.md](README.md) Documentation section gained 3 new links (faq, standard-vs-ours, extended troubleshooting summary). | Operator: nothing new blocking. Manual-install-guide is now scoped and ready to build IF a tester or prospect asks. All Session 9 carry-forwards remain. |
| 12 | 2026-06-03 | Pre-beta triage scaffolding. Session continuation was tagged "triage tester feedback against Stage 3 A/B/C buckets" but the private beta has not yet started — operator-side prerequisites from Session 9 row (GitHub push / test matrix / Recovery Drill / DPA / privacy + install pages / Loom) still gate Stage 1 invitations per [private-beta-gate.md](release/private-beta-gate.md) Prerequisites. Rather than triage on no input, prepped the capture+triage surface so feedback can land into a defined shape the moment first tester replies. Created [release/beta-feedback-runs/_template.md](release/beta-feedback-runs/_template.md) (cohort header, capture rules, feedback table with verbatim/tag/bucket/action columns, full Stage 3 rubric inline as cheat-sheet incl. edge-call rules, Stage 4 decision checklist, linked-fixes tables for Bucket A + B, pointer to backlog for C). Created [release/post-launch-backlog.md](release/post-launch-backlog.md) (Bucket C destination + "deferred-from-B" rows with explicit decision audit; immutable-class rule; review-cadence rule). Patched private-beta-gate.md Stage 2 step 2 to point at the new template, and Stage 3 Bucket C action to wikilink the backlog. No other changes. | Operator-side: all Session 9 carry-forwards remain blocking for Stage 1 invitations (GitHub push, info@ monitoring, test matrix execution, Recovery Drill, Supabase DPA, privacy + install page deploys, Loom). The triage workflow itself is now operator-ready: when first tester replies, copy _template.md → release/beta-feedback-runs/<date>.md, fill rows, hold triage 2-3 days after last reply. |
| 7 | 2026-06-02 | URL-discipline sweep (Rob asked: "do we have to have live URLs on the readme? can we not just direct people to the website and an email address?"). Removed speculative URLs across the bundle; only two URLs are now load-bearing and committed: `absolutionlabs.com` (homepage, present-tense) + `absolutionlabs.com/privacy` (load-bearing for the telemetry story). Everything else routes through "see the install page on absolutionlabs.com" + "email info@absolutionlabs.com". Files touched: [README.md](README.md) (install section reframed — website is canonical install surface, no version-pinned URLs in body; feedback merged into support email; signing-key / checksums references genericised to "linked from the install page"), [SKILL.md](SKILL.md) (Step 5 privacy URL kept as markdown link, Step 9.2 feedback URL removed + merged with support, when-to-invoke trigger genericised), [loom-script.md](loom-script.md) (Scene 1 install-page reference simplified, Scene 6 privacy line shortened, Scene 10 feedback URL merged with support email, post-recording description block simplified), [plugin.json](plugin.json) (`homepage` simplified to absolutionlabs.com; `repository` set to null pending GitHub setup; `checksums_url` + `release_signing` set to null with `_release_artifacts_note` explaining they finalise at Chunk 7; `trigger_url_paste` genericised), [docs/upgrading.md](docs/upgrading.md) (4 sections rewritten — version pinning reframed around install-page selector instead of URL pattern, Cowork + Code upgrade flows point at install page, rollback flow same, version retention rewritten, discovery section simplified to 3 channels), [docs/troubleshooting.md](docs/troubleshooting.md) (2 install-URL examples genericised). Verification: `grep -n "absolutionlabs.com/(install|feedback|changelog|forge|signing)"` returns no matches. Spirit: the README is a technical artifact for someone who already has the bundle; the website is the install funnel + per-version surface. Two surfaces, two jobs. | Chunk 6b (website surfaces) now consumes the URL structure that this sweep deferred — the install page must show the install URL, the version picker, the Loom embed, the per-version release notes link, and a checksums/signing-key surface for each version. Other open items unchanged. Suggested next still: Chunk 6b. |

## Deliverables

| File / artifact | Status | Notes |
|---|---|---|
| `current-setup-user-guide.md` (in project folder) | Draft v0.1 | Needs minor update to align with sales-tool framing |
| `install-steps-and-skill-feasibility.md` (in project folder) | Draft v0.1 | Stable; informs build |
| `shape-1-scope.md` (in project folder) | Draft v0.1 | Needs update with locked decisions + appetite change |
| `brief.md` (this file) | Shape | Live |
| Skill bundle (Cowork plugin + Code skill from one source) | **Complete** | Chunks 2–7 all landed by session end 2026-06-03 |
| Templates folder (vault scaffold content) | **Complete** | Chunk 2 (16 files; Session 1) |
| SKILL.md procedure | **Complete** | Chunk 3 (Session 2; `SKILL.md` at project root, ~430 lines) |
| Manifests (plugin.json + Code skill frontmatter) | **Complete** | Chunk 4 (Session 2; `plugin.json` + `MANIFESTS.md` mirror contract; Code-side manifest = SKILL.md frontmatter) |
| Lint procedure (manifest lint) | **Complete** | Chunk 5 (Session 2; `scripts/lint_manifest.py`, 11 checks, runs 10/10 green against live bundle) |
| Telemetry endpoint (Supabase PostgREST + RLS) | **LIVE** | Chunk 5b redone on Supabase 2026-06-03 (Option B). Project `vujwcvqiwwpncnhgxjsu` on eu-west-2; migration applied; smoke-tested end-to-end. Bundle wired through plugin.json telemetry block. Operator follow-ups: DPA, custom domain (deferred). |
| Privacy policy at absolutionlabs.com | **Draft updated for Supabase; publish pending** | Chunk 5b (Session 2; `docs/privacy-policy.md`, ~170 lines). Provider, sub-processor, region, security posture all updated 2026-06-03 to match the live Supabase setup. Legal review explicitly deferred by Rob; publishable as-is when website surface lands. |
| LICENSE + README + COMPATIBILITY.md + docs/* (bundle-level docs) | **Complete** | Chunk 6 (Session 2; LICENSE [MIT], README.md, COMPATIBILITY.md, docs/customisation.md, docs/troubleshooting.md, docs/upgrading.md; ~735 lines total) |
| Website-level surfaces (install landing page, embedded Loom, feedback form spec, privacy page integration) | **Complete (copy)** | Chunk 6b (Session 2; `website/` folder — README + install-page + privacy-page-deploy + feedback-form-spec + loom-embed-snippet). Operator deploys to absolutionlabs.com via Netlify. |
| Loom guided-install script | **Complete** | Chunk 6.5 (Session 2; `loom-script.md`, ~290 lines) |
| Loom recording (operator deliverable) | Not started | Blocked only on privacy URL being live. Once published, ~30 min to record per `loom-script.md` Section "Pre-recording prep checklist". |
| Tester instructions | **Complete** | `TESTERS.md` at bundle root (Session 2; ~170 lines for the 3-5 friendly tester cohort) |
| Test matrix (7 personas) | **Documented** | Chunk 7 (Session 2; `release/test-matrix.md` — runnable spec with Setup / Install procedure / Pass criteria per persona). Execution = operator. ~5h time estimate. |
| Private beta gate procedure | **Complete** | Chunk 7 (Session 2; `release/private-beta-gate.md` — 5-stage process for moving from beta → public ship) |
| Recovery Drill procedure | **Complete** | Chunk 7 (Session 2; `release/recovery-drill.md` — 9-step drill, log table awaiting first run) |
| Public install URL | Not started | Operator: `git init` + push to public GitHub for the install URL surface |

## Impact Metric

Two metrics, both binary, both proxied for trust:

1. **Install→working-vault success rate ≥ 95%** across all opt-in-telemetry'd installs in the first 3 months. Measures whether the skill works for prospects.
2. **Zero data-loss or compliance incidents reported** in the first 6 months. Measures whether the skill avoids damaging prospects.

Adoption volume is explicitly NOT a metric.

## Recovery Drill Log

Required before Gate 3 (production-touching project — telemetry endpoint + privacy-policy infra count).

| Date | What was restored | From | Time-to-restore | Result | Notes |
|---|---|---|---|---|---|
| | | | | | |

## Keep / Stop / Start

[Fill in after SHIP phase.]

- **Keep:**
- **Stop:**
- **Start:**
